# Phase 5 — Interactive Map (Leaflet + OpenStreetMap)

> **Goal:** Embed an interactive Leaflet map showcasing quiet zones, user pins, and community spot highlights.

## Deliverables

- [x] Leaflet map mounting and asset compilation setup (`client/src/map/leafletMap.js`)
- [x] Color-coded live noise signal markers (`client/src/map/markers.js`)
- [x] Saved user location markers (`client/src/map/userPins.js`)
- [x] Floating overlay control panel:
  - [x] Back to home button
  - [x] Nominatim search bar with forward geocoding zoom
  - [x] Live, My Pins, and Community layer toggles
- [x] Floating legend map key overlay
- [x] CSS Map Layout stylesheet (`client/src/map/map.css`)
- [x] Safe afterMount routing hook to initialize Leaflet map safely within the DOM

## Status: ✅ Complete
